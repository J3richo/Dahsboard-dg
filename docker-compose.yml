services:
  dashboard-dg:
    image: nginx:alpine
    container_name: dashboard-dg
    ports:
      - "8085:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
